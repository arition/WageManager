<controls:MetroWindow x:Class="WageManager.Company"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="公司" Height="300" Width="500" Closing="MetroWindow_Closing" Loaded="MetroWindow_Loaded">
    <Grid>
        <DataGrid x:Name="DataGrid_company" Visibility="Hidden">
            <DataGrid.ContextMenu>
            <ContextMenu>
                <MenuItem x:Name="MenuItem_delete" Header="删除" Click="MenuItem_delete_Click"/>
            </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
        <controls:ProgressRing x:Name="ProgressRing_company" IsActive="True" Panel.ZIndex="1" />
    </Grid>
</controls:MetroWindow>
